function addToCart(productId) {
    alert(`Product ${productId} added to cart!`);
    // Add logic to update cart in session/local storage
}

function checkout() {
    alert("Proceeding to checkout!");
    // Add logic to handle checkout
}